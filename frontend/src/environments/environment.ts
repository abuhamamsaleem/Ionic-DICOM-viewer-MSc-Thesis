// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  version: '1.0.0',
  firebase: {
    apiKey: 'AIzaSyC5-Fon1LlopfxopNxUVAzGsgHPwU62R4g',
    authDomain: 'radipacs-dev.firebaseapp.com',
    databaseURL: 'https://radipacs-dev-default-rtdb.firebaseio.com/',
    projectId: 'radipacs-dev',
    storageBucket: 'radipacs-dev.appspot.com',
    messagingSenderId: '510561361172',
    appId: '1:510561361172:web:c3b3418ead991301aa7601',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
